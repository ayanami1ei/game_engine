#include"measurement_unit/displacement_unit.h"

using namespace MeasurementUnit::DisplacementUnit;

illegal_into(KiloMeter)
    illegal_into(Meter)
        illegal_into(MilliMeter)
            illegal_into(MicroMeter)
